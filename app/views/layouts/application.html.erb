<!DOCTYPE html>
<html>
	<head>
	  <title>AuditControlEntry</title>
	  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
	  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
	  <%= csrf_meta_tags %>
	</head>

	<body>
		<%= content_tag :div, :class => "navbar navbar-inverse" do %>
			<%= content_tag :div, :class => "navbar-inner"  do%>
				<%#= link_to "Claim List", set_params_claim_audit_lists_path, :class => "btn btn-primary pull-right" unless controller_name == "claim_audit_lists"%>
			<% end %>
		<% end %>
		
		<%= content_tag :div, :class => "clearfix", :id => "main-content" do%>
		    <%= content_tag :div, :class => "clearfix", :id => "page-content" do%>
				<%= yield %>
			<% end %>
		<% end %>
	</body>

</html>
